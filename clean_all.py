import os
import pymupdf

originalpdf dir="YOUR-PDF-FOLDER" 
cleanedpdf dir="OUTPUT FOLDER"

for pdf_file in os.pdf.dir(originalpdf dir):
    if pdf_file.endswith(".pdf"):
        pymupdf clean [-h][-password PASSWORD] 
		[-encryption {none}]
		[-sanitize]
		input output

        with open(cleanpdf, "r", encoding="utf-8") as f:
            content = pdf.load(f)
